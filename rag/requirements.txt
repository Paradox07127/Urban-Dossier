# OpenAI-compatible client for the vLLM-served embedding instance
# (Qwen3-Embedding-4B by default, served on :8001 alongside Nemotron on :8000).
openai>=1.50

# CrossEncoder reranker (BAAI/bge-reranker-v2-m3, lazy-loaded).
sentence-transformers==3.0.1

# Numerical stack.
numpy>=1.26
pydantic>=2.5

# Vector index: FAISS-CPU is the dev/Mac fallback when cuvs is not installed.
# On DGX Spark install cuvs to make CuvsIndex the GPU default:
#   pip install cuvs-cu13              # preferred if wheel available for ARM64
#   conda install -c rapidsai cuvs    # fallback
faiss-cpu==1.8.0
